{
  "name": "nebula-loader",
  "version": "2.1.0",
  "description": "Display a loading progress overlay.",
  "license": "ISC",
  "private": true,
  "author": "Chris Beckett <chrisb@arsnebula.com>",
  "homepage": "https://github.com/arsnebula/nebula-loader",
  "repository": "arsnebula/nebula-loader",
  "scripts": {
    "test": "wct --plugin=local",
    "test:debug": "wct --plugin=local --persistent=true",
    "test:sauce": "wct --plugin=sauce",
    "start": "browser-sync start -c bsconfig.js"
  },
  "devDependencies": {
    "browser-sync": "latest",
    "web-component-tester": "^6.0.0"
  }
}
